DB_USER=postgres
DB_PASSWORD=your_password_here
JWT_SECRET=your-secret-key-must-be-at-least-32-characters-long
CONFIG_PATH=./config/local.yaml

# Generate strong JWT_SECRET with:
# openssl rand -hex 32
# or
# head -c 32 /dev/urandom | base64
